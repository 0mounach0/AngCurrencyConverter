import { CurrencySwitchDirective } from './currency-switch.directive';

describe('CurrencySwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencySwitchDirective();
    expect(directive).toBeTruthy();
  });
});
